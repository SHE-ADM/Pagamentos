# Backend local do pagamentos (server/app.py)
# A lógica de leitura vive em skills/email-reader/scripts/read_emails.py e a
# extração em skills/pdf-contas-pagar/scripts/extract_pdf.py — por isso o
# pipeline depende de todos os pacotes abaixo, não só do Flask.
# Versões fixadas com ~= (compatível no minor) para dev/prod não divergirem.

# Servidor HTTP + carregamento do .env
flask~=3.1
python-dotenv~=1.2

# Extração de PDF (texto nativo)
pdfplumber~=0.11.9
Pillow~=12.2

# Extração via Claude API + montagem de CSV
anthropic~=0.105.2
pandas~=3.0

# Driver Firebird 5 — skill cobranca-vencidos (db_firebird.py). Requer o Firebird
# Client nativo instalado no servidor; db_firebird cai para firebirdsql se ausente.
fdb~=2.0.4
